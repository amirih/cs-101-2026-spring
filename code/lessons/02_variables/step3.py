
if __name__ == '__main__':
    first_name = "John"
    last_name = "Doe"
    print(f"first_name: {first_name}, last_name: {last_name}")

    # in python, you can use any character except for the following:
    # ' " , : ; < > / \ | ? ! @ # $ % ^ & * ~ ` ( ) [ ] { }
    # you can also not use spaces
    # you can use numbers but not at the beginning of the variable name

    # you can use underscores to separate words
    # this is called snake_case
    class_name = "Python Programming"
    print(f"class_name: {class_name}")
